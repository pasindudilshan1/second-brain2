---
description: Append a timestamped note to wiki/log.md
argument-hint: the thought or note to capture
---

Append the following note to wiki/log.md:

$ARGUMENTS

1. Append a timestamped entry (format: `- YYYY-MM-DD HH:MM: $ARGUMENTS`) to wiki/log.md.
2. Check if the note mentions any project, person, or concept that has an existing wiki page:
   - If yes, update that page to reference this log entry or incorporate the note
   - If no, do NOT create new pages - just append to the log
3. Confirm the entry was added and list any pages that were updated as a result.
